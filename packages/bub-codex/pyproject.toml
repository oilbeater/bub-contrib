[project]
name = "bub-codex"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Frost Ming", email = "me@frostming.com" }
]
requires-python = ">=3.12"
dependencies = [
    "bub>=0.3.0",
]

[project.entry-points.bub]
codex = "bub_codex.plugin"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
